server:
  port: 8080

spring:
  application:
    name: BusTicketBooking

  datasource:
    url: jdbc:mysql://localhost:3306/busticketbooking
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false