server:
  port: 8080

spring:
  application:
    name: BusTicketBooking

  datasource:
    url: jdbc:mysql://localhost:3306/busticketbooking
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false