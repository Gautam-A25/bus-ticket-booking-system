spring:
  datasource:
    url: jdbc:mysql://localhost:3306/busticketbooking_test
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: AayushCode@5252

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: test-secret-key-1234567890
  expiration-ms: 86400000